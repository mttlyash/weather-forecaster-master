{
  "name": "weather-forecast",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "client-dev": "cd client && npm i && npm start",
    "server-dev": "cd server && npm i && npm run dev",
    "build": "cd client && npm i && npm run build && cd ../server && npm i",
    "start": "cd server && npm start"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "16.14.0"
  },
  "dependencies": {
    "typescript": "^4.9.4"
  },
  "devDependencies": {
    "@types/node": "^18.11.18"
  }
}
